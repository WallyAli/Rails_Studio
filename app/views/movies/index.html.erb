<h3><%= @movies.size %> Movies</h3>

<ul>
	<% @movies.each do |movie| %>
		<li><strong><%= movie.title %></strong> 
			<%= movie.rating %> :
			<%= number_to_currency(movie.total_gross) %></li>
	<% end %>
</ul>

